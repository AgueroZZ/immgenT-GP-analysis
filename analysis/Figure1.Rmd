---
title: "Figure 1: A gene-program architecture underlies T cell heterogeneity across the immgenT atlas"
author: "Ziang Zhang"
output: workflowr::wflow_html
editor_options:
  chunk_output_type: console
---

```{r doc-code, include=FALSE}
source("../code/R/doc_code.R")
```

All panels except (b) are produced by
[`script/Figure1.R`](https://github.com/AgueroZZ/immgenT-GP-analysis/blob/main/script/Figure1.R).
The code below is shown for reference; the images are its pre-rendered output.

## Setup

```{r fig1-setup1, code=code_for("../script/Figure1.R", c("doc:setup", "Load data")), eval=FALSE}
```

```{r fig1-setup2, code=code_for("../script/Figure1.R", "doc:setup2"), eval=FALSE}
```

## (a) All-T MDE by lineage {#fig1a}

```{r fig1a-code, code=code_for("../script/Figure1.R", "1A"), eval=FALSE}
```

```{r fig1a-img, echo=FALSE, out.width="50%"}
knitr::include_graphics("assets/Figure1/1A.png")
```

::: {.figcaption}
**Fig. 1a.** Minimum distortion embedding (MDE) plot of all mature T cells
profiled in immgenT (all-T MDE), colored by T cell lineage: CD4, conventional
CD4⁺ T cells; CD8, conventional CD8αβ⁺ T cells; Treg, CD4⁺ Foxp3⁺ regulatory T
cells; CD8aa, CD8aa αβ T cells; gdT, gdT cells; Tz, Zbtb16⁺ αβ T cells; DN,
CD4⁻CD8αβ⁻ αβ T cells; DP, CD4⁺CD8αβ⁺ αβ T cells.
:::

## (b) GP-representation schematic {#fig1b}

A hand-drawn schematic, not generated from code. The factorization itself is
documented in [Methods: fitting EBMF using flashier](Methods_FlashierFit.html).

::: {.figcaption}
**Fig. 1b.** Schematic of the gene-program (GP) representation of T cell
gene-expression profiles. Each cell is represented as a linear combination of
gene programs, and each GP is defined by the upregulation or downregulation of
a subset of genes. GPs are learned using EBMF, which decomposes the expression
of gene $j$ in cell $i$ ($x_{ij}$) as a sum over $K$ gene programs, $x_{ij} =
l_{i1}f_{j1} + \ldots + l_{iK}f_{jK}$, where $l_{ik}$ is the activity level of
program $k$ in cell $i$, and $f_{jk}$ is the effect of GP $k$ on expression of
gene $j$.
:::

## (c) Gene-program network {#fig1c}

```{r fig1c-code, code=code_for("../script/Figure1.R", "1C"), eval=FALSE}
```

```{r fig1c-img, echo=FALSE, out.width="70%"}
knitr::include_graphics("assets/Figure1/1C.png")
```

::: {.figcaption}
**Fig. 1c.** GP-gene network in which all 200 GPs are linked to their five
most strongly regulated genes. Grey nodes are GPs, and tan nodes are genes.
:::

## (d) GP loading heatmap {#fig1d}

```{r fig1d-code, code=code_for("../script/Figure1.R", "1D"), eval=FALSE}
```

```{r fig1d-img, echo=FALSE, out.width="100%"}
knitr::include_graphics("assets/Figure1/1D.png")
```

::: {.figcaption}
**Fig. 1d.** Heatmap showing the activity of the 200 GPs across T cells from
different lineages and activation states (excluding cells in "miniverse").
:::
