---
title: "Figure 3: T cell lineage-related GPs"
author: "Ziang Zhang"
output: workflowr::wflow_html
editor_options:
  chunk_output_type: console
---

```{r doc-code, include=FALSE}
source("../code/R/doc_code.R")
```

All panels are produced by
[`script/Figure3.R`](https://github.com/AgueroZZ/immgenT-GP-analysis/blob/main/script/Figure3.R).
The code below is shown for reference (not re-executed on this page); the
images are its pre-rendered output.

## Setup

Data loading, shared across all panels below.

```{r fig3-setup, code=code_for("../script/Figure3.R", c("^", "Load data")), eval=FALSE}
```

## (A) AUC swarm plot by lineage {#fig3a}

```{r fig3a-code, code=code_for("../script/Figure3.R", "3A"), eval=FALSE}
```

```{r fig3a-img, echo=FALSE, out.width="60%"}
knitr::include_graphics("assets/Figure3/3A.png")
```

::: {.figcaption}
**Fig. 3A.** Swarm plot showing the AUC of each GP for predicting major T cell
lineage (one-vs-rest AUC, computed on healthy non-thymocyte cells) across the
seven major lineages (CD8, CD4, Treg, gdT, CD8aa, Tz, DN). Only GPs
up-regulated in a given lineage (mean loading >= the overall mean) are plotted
for it as circles. The most predictive GP per lineage is labeled -- GP58 (CD8),
GP74 (CD4), GP68 (Treg), GP3 (gdT), GP29 (CD8aa), GP30 (Tz) and GP22 (DN) -- as
are GP3, GP29 and GP22 in each of gdT, CD8aa and DN; all three are shown in all
three of those lineages, with triangles marking the ones that are
down-regulated there. The dashed line marks AUC = 0.5.
:::

## (B) Structure plot of lineage-defining GPs {#fig3b}

```{r fig3b-code, code=code_for("../script/Figure3.R", "3B"), eval=FALSE}
```

```{r fig3b-img, echo=FALSE, out.width="60%"}
knitr::include_graphics("assets/Figure3/3B.png")
```

::: {.figcaption}
**Fig. 3B.** Structure plot showing single-cell activity (membership / GP loading)
of the lineage-related GPs identified in (A), grouped by lineage (CD8, CD4, Treg,
gdT, CD8aa, Tz, DN); CD4 and CD8 cells are subsampled for visualization. Six of
the seven most-predictive GPs from (A) are shown, one per lineage -- GP58 (CD8),
GP68 (Treg), GP30 (Tz), GP3 (gdT), GP29 (CD8aa) and GP22 (DN). CD4's top GP in
(A), GP74, is not included: no single GP marks conventional CD4 cells strongly
(its AUC is only ~0.68, the lowest of the seven).
:::

## (C) GP68 activity on the all-T MDE {#fig3c}

```{r fig3c-code, code=code_for("../script/Figure3.R", "3C/3E/3G"), eval=FALSE}
```

```{r fig3c-img, echo=FALSE, out.width="40%"}
knitr::include_graphics("assets/Figure3/3C.png")
```

::: {.figcaption}
**Fig. 3C.** GP68 activity on the all-T MDE; each point is a cell, colored by its GP68 activity (loading).
:::

## (D) GP68 signature volcano {#fig3d}

```{r fig3d-code, code=code_for("../script/Figure3.R", "3D/3F/3H"), eval=FALSE}
```

```{r fig3d-img, echo=FALSE, out.width="40%"}
knitr::include_graphics("assets/Figure3/3D.png")
```

::: {.figcaption}
**Fig. 3D.** Gene scores for GP68. The x axis shows the GP68 gene score, scaled as in Figure 2A (per-GP maximum |score| = 1), and the y axis shows mean shifted-log expression across all T cells; top genes are labeled.
:::

## (E) GP30 activity on the all-T MDE {#fig3e}

```{r fig3e-code, code=code_for("../script/Figure3.R", "doc:3e"), eval=FALSE}
```

```{r fig3e-img, echo=FALSE, out.width="40%"}
knitr::include_graphics("assets/Figure3/3E.png")
```

::: {.figcaption}
**Fig. 3E.** GP30 activity on the all-T MDE; each point is a cell, colored by its GP30 activity (loading).
:::

## (F) GP30 signature volcano {#fig3f}

```{r fig3f-code, code=code_for("../script/Figure3.R", "doc:3f"), eval=FALSE}
```

```{r fig3f-img, echo=FALSE, out.width="40%"}
knitr::include_graphics("assets/Figure3/3F.png")
```

::: {.figcaption}
**Fig. 3F.** Gene scores for GP30. The x axis shows the GP30 gene score, scaled as in Figure 2A (per-GP maximum |score| = 1), and the y axis shows mean shifted-log expression across all T cells; top genes are labeled.
:::

## (G) GP58 activity on the all-T MDE {#fig3g}

```{r fig3g-code, code=code_for("../script/Figure3.R", "doc:3g"), eval=FALSE}
```

```{r fig3g-img, echo=FALSE, out.width="40%"}
knitr::include_graphics("assets/Figure3/3G.png")
```

::: {.figcaption}
**Fig. 3G.** GP58 activity on the all-T MDE; each point is a cell, colored by its GP58 activity (loading).
:::

## (H) GP58 signature volcano {#fig3h}

```{r fig3h-code, code=code_for("../script/Figure3.R", "doc:3h"), eval=FALSE}
```

```{r fig3h-img, echo=FALSE, out.width="40%"}
knitr::include_graphics("assets/Figure3/3H.png")
```

::: {.figcaption}
**Fig. 3H.** Gene scores for GP58. The x axis shows the GP58 gene score, scaled as in Figure 2A (per-GP maximum |score| = 1), and the y axis shows mean shifted-log expression across all T cells; top genes are labeled.
:::

## (I) gdT/CD8aa/DN MDE by lineage {#fig3i}

```{r fig3i-code, code=code_for("../script/Figure3.R", "3I"), eval=FALSE}
```

```{r fig3i-img, echo=FALSE, out.width="40%"}
knitr::include_graphics("assets/Figure3/3I.png")
```

::: {.figcaption}
**Fig. 3I.** All-T MDE subsetted to show only γδ T cells, CD8aa T cells and DN T
cells (excluding proliferating and miniverse subsets), colored by lineage: γδ T
in green, CD8aa in purple, and DN in blue.
:::

## (J-L) GP3, GP29, GP22 loading on the gdT/CD8aa/DN MDE {#fig3jl}

```{r fig3jl-code, code=code_for("../script/Figure3.R", "3J/3K/3L"), eval=FALSE}
```

```{r fig3jl-img, echo=FALSE, out.width="30%"}
knitr::include_graphics(c("assets/Figure3/3J.png", "assets/Figure3/3K.png", "assets/Figure3/3L.png"))
```

::: {.figcaption}
**Fig. 3J-L.** GP activity on the same gdT/CD8aa/DN MDE for GP3 (J), GP29 (K), and GP22 (L) -- the same GP order as the columns of (M).
:::

## (M) Top-30 gene score heatmap for GP3/GP29/GP22 {#fig3m}

```{r fig3m-code, code=code_for("../script/Figure3.R", "3M"), eval=FALSE}
```

```{r fig3m-img, echo=FALSE, out.width="40%"}
knitr::include_graphics("assets/Figure3/3M.png")
```

::: {.figcaption}
**Fig. 3M.** Heatmap of the 30 most up-regulated genes across GP3, GP29 and GP22: of the genes with a positive score in at least one of the three, the 30 with the highest score across them. Gene scores are scaled so that each GP's largest absolute score is 1 (blue, negative; red, positive), and a gene selected for one GP can still be negative in another. Rows are hierarchically clustered, with Fcer1g, Ccl5, Cd7 and Ctsw pinned to the top.
:::
