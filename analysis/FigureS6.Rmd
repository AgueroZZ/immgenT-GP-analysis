---
title: "Extended Data Figure 6: CD69-associated GPs and further surface-protein gating strategies"
author: "Ziang Zhang"
output: workflowr::wflow_html
editor_options:
  chunk_output_type: console
---

```{r doc-code, include=FALSE}
source("../code/R/doc_code.R")
```

All panels are produced by
[`script/FigureS6.R`](https://github.com/AgueroZZ/immgenT-GP-analysis/blob/main/script/FigureS6.R),
which shares its CITE-seq setup, gating logic and CD69 GP subset with
[Figure 6](Figure6.html) via `code/R/citeseq_shared_setup.R` and
`code/R/gated_protein_helpers.R`. The code below is shown for
reference (not re-executed on this page, since this script takes about a minute
to run); the images are its pre-rendered output.

## Setup

```{r figs6-setup, code=code_for("../script/FigureS6.R", "doc:setup"), eval=FALSE}
```

## (a, b) CD69-associated GPs across tissues and lineages {#figs6ab}

The ten GPs and their order are defined in `code/R/citeseq_shared_setup.R`, so
that these two panels and [Fig. 6d](Figure6.html#fig6d) -- which live in
different figures and therefore in different scripts -- cannot disagree about
which GPs they show or in what order:

```{r figs6ab-gps, code=code_for("../code/R/citeseq_shared_setup.R", "doc:cd69"), eval=FALSE}
```

```{r figs6ab-code, code=code_for("../script/FigureS6.R", "s6a"), eval=FALSE}
```

```{r figs6ab-img, echo=FALSE, out.width="49%"}
knitr::include_graphics(c("assets/FigureS6/s6a.png", "assets/FigureS6/s6b.png"))
```

::: {.figcaption}
**Extended Data Fig. 6a, b.** Mean activity of the ten CD69-associated GPs of
[Fig. 6d](Figure6.html#fig6d), in the same order, **(a) across tissues**
(`organ_simplified`) and **(b) across lineages** (`annotation_level1`). That
order is the Spearman correlation between GP activity and CD69 protein
expression. Color runs from white (zero) to firebrick (each panel's largest mean
activity). The ten GPs are a **hand-picked** set drawn from among the GPs most
strongly correlated -- positively or negatively -- with CD69, not a computed
top-10; `script/verify_cd69_gp_ranking.R` recomputes the correlation over all 200
GPs and fails if this wording and the code drift apart.
:::

## (c-f) Further protein-gated vs. GP-loading populations {#figs6cf}

```{r figs6cf-code, code=code_for("../script/FigureS6.R", "s6c"), eval=FALSE}
```

```{r figs6cf-img, echo=FALSE, out.width="49%"}
knitr::include_graphics(c(
  "assets/FigureS6/s6c.png", "assets/FigureS6/s6d.png",
  "assets/FigureS6/s6e.png", "assets/FigureS6/s6f.png"
))
```

::: {.figcaption}
**Extended Data Fig. 6c-f.** Examples of gating strategies used to identify GP-active cells, as
in [Fig. 6e-j](Figure6.html#fig6ej), for **(c) GP29, (d) GP58, (e) GP22 and
(f) GP68**. For each GP, all-T MDE plots highlight cells passing a protein gate
built from the GP's curated marker signature (left; positive markers above
threshold, negative markers at or below, with the gate size reported in the panel
title as "Matched n") and, on the right, an equally sized set of cells with the
highest GP activity -- exactly as many cells as the protein gate selected. Color
indicates cell density (two-dimensional); all other cells are grey. Thymocytes,
proliferating, and "miniverse" cells are excluded. Each panel is labeled with its
GP and the markers the gate actually applied; every marker in these four
signatures has a manually reviewed positivity threshold, so none is silently
dropped.
:::
